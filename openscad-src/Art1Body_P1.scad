
////////////////////////////////////////////////////////////////////////////
// AUXILIARY MODULES
module cylinder_outer(height,radius,fn,cen) {
   fudge = 1/cos(180/fn);
   cylinder(h=height,r=radius*fudge,$fn=fn,center=cen);
}

////////////////////////////////////////////////////////////////////////////
// RENDERS

print_part();

////////////////////////////////////////////////////////////////////////////
// MAIN MODULES

module print_part() {
  render(convextity=1)
    import("ART1BODY_P1.stl",convexity=0.5);
}